# tubetell reads these from the environment, or from a .env file in the
# directory you run it from. Real environment variables win over .env.

# Required for all modes: your GCP project (Vertex AI API must be enabled).
GOOGLE_CLOUD_PROJECT=my-project-id

# Optional; defaults to "global".
GOOGLE_CLOUD_LOCATION=global

# Optional: path to a service-account key. If unset, tubetell uses your
# local Application Default Credentials (gcloud auth application-default login).
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/key.json

# Required only for --mode comments (YouTube Data API v3).
# YOUTUBE_API_KEY=...
